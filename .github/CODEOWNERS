# Reserved paths for the Systematic-Review blinding wall.
#
# The blinded base tables and the blinding chokepoint that is the only code
# allowed to read them require an explicit review from the code owner. This is
# the human backstop behind the Postgres privilege wall, the ESLint
# no-restricted-imports rule, and the CI grep guard.
# See FOUNDATION-auth-tenancy.md §6.

# The three blinded base tables (screening_decisions, extraction_entries, rob_assessments).
/src/lib/db/schema/sr-blinded.ts   @drshailesh88

# The blinding chokepoint — the only intended reader of the blinded tables (T2).
/src/lib/sr/authz/                  @drshailesh88

# The privilege-wall migration and its acceptance test.
/drizzle/0002_sr_privilege_wall.sql @drshailesh88
/scripts/check-blinded-wall.mjs     @drshailesh88
/scripts/test-blinded-wall.sh       @drshailesh88
