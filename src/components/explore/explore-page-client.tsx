'use client';

// Stub only — Task C6 fills this in with the real search state machine.
export function ExplorePageClient({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  initialQuery,
}: {
  initialQuery: string;
}) {
  return null;
}
