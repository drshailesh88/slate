# Editor / Manuscript — States Design Spec (+ Ask⇄Draft, act-on-selection)

The writing surface. **This is where the `Ask ⇄ Draft` blast-radius toggle finally lives** (it earns its place only where "will it edit my document?" is a live question — see the composer decision log). Heavy user of `CRAFT-ADDENDUM.md §B` (in-editor composer, act-on-selection, universal generation row, accent-draft-then-accept).
**Authorities:** `design.md` (skin, esp. the manuscript reference `docs/design/reference/editor-manuscript.html`) · `CRAFT-ADDENDUM.md §B` · `STATE-INVENTORY.md`.

---

## 1. Surface anatomy

- **Focus-first shell** — the editor is a focus context: the sidebar can **full-hide** (design.md), leaving a minimal focus top-bar: breadcrumb (`Project › Draft`), `Exit`, and an `Autosaved · just now` indicator (`--muted`, `--mono` time).
- **Document column** — centered, comfortable measure (~65–72ch), generous margins. Headings = Source Serif; body = design.md reading type; numerals/stats `--mono`. Inline citations render as `[n]` chips (`--accent`, link affordance).
- **The `Ask ⇄ Draft` toggle** — a small, always-visible control on the doc's AI affordance (composer bar or right-rail head): **Ask** = read-only (answers in the right rail, touches nothing) · **Draft** = it can write into the manuscript/citations. The one control that says whether the AI will touch your work.
- **Right-rail assistant** (optional, Ask mode) — docked Ask-AI panel (addendum §B states: greeting+starters → typing → thinking → answer + action row).

---

## 2. The AI-writing contracts (from CRAFT-ADDENDUM.md §B)

- **In-editor composer (block-anchored)** — invoke on a block (`/ai` or the block grip's "Ask AI"): an **edit-bar docked to that block**, not a floating modal. Lifecycle: **rest → typing → shimmer → draft rendered in-place in `--accent` (uncommitted) → Accept / Discard / Insert-below / Try-again → strip to `--ink` on accept.**
- **Act-on-selection** — select prose → **floating toolbar** above the selection (format icons + `Explain` · `Ask AI`) → **grouped menu: Suggested (contextual) vs Edit (shorter/longer/tone/translate/fix)** → **diff-view for edit-type transforms** (strikethrough + accent insert), **outright replace for generate-type**. One trust line at the card foot: `Uses AI. Verify results.` (mandatory per provenance §6).
- **Universal generation action-row** — every AI output (rail, inline, or a Deep-Research report inserted here) ends in the same fixed row: `Copy · Insert · Try again`.
- **Citations** — insert via `/citation` or the composer; a citation is an `--accent [n]` chip; **hover → source Side-peek** (the cited paper); a citation-check block can sit inline as a regenerable AI block (`Generated by AI · Update`) marking unsupported claims.

---

## 3. States (desktop + mobile)

| State | Spec |
|---|---|
| **Empty draft (first-run)** | Blank doc, title placeholder `Untitled`, body placeholder `Start writing, or press / for AI`. Empty-state-teaches-the-move: a subtle `/` hint + `Draft from your sources →` CTA. |
| **Writing (populated)** | The manuscript with content, inline `[n]` citations, autosave running. Calm; chrome hidden. |
| **In-editor composer active** | The block-anchored edit-bar open on a block; §B lifecycle. Rest of doc dims slightly (Continuity, opacity only). |
| **AI drafting into manuscript** | Streamed prose appears **in-place in `--accent`** (uncommitted), a response toolbar above it; `--motion` = Status shimmer while streaming, Continuity accent→ink on accept. The `Draft` toggle must be on; in `Ask` mode this is blocked and the answer goes to the rail instead. |
| **Act-on-selection active** | Floating toolbar + Suggested/Edit menu + diff-or-replace card, anchored to the selection. |
| **Citation hover / manager** | Hover `[n]` → source Side-peek; a `Citations` panel lists all, flags unresolved. |
| **Saving / offline** | `Autosaved · just now` normal; offline → `Saved locally — will sync when you reconnect` (Amber, `--muted`); nothing is lost. |
| **Error** | Save/generation failure → inline Tomato note at the affected block: `Couldn't save this change — retry` (never a full-screen wall; the doc stays editable). |

### Mobile
Full-screen document; the `Ask ⇄ Draft` toggle + `/` AI live in a bottom composer bar; act-on-selection toolbar appears above the selection as a horizontally-scrollable pill; source peek → full-screen sheet; focus top-bar collapses to a back-chevron + autosave dot.

---

## 4. Motion (design.md §8)
- Block edit-bar open → **Orient** (grows from the block edge). Doc dim → **Continuity** (opacity).
- Streaming draft → **Status** shimmer; accept accent→ink → **Continuity** (opacity swap, not a color tween).
- Selection toolbar appear → **Orient** `--motion-micro`.
- Accept/Insert/Try-again, save toast → **Feedback**.
- `prefers-reduced-motion` → instant.

## 5. Build order
Empty draft → Writing → in-editor composer lifecycle (rest→draft→accept) → act-on-selection (toolbar→menu→diff/replace) → citation hover/peek → saving/offline/error → mobile.
